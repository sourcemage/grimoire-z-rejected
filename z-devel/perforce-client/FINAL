# Clean out the source. We can't cache it because the filenames (p4/p4web etc)
# are the same whatever the version. There seems no way to work around this
# at the moment.
rm -f $SOURCE_CACHE/$SOURCE  &&
rm -f $SOURCE_CACHE/$SOURCE2 
