if  spell_installed  ttmkfdir ; then
  if  [ -d  /usr/share/fonts/truetype ] ; then
    cd  /usr/share/fonts/truetype  &&
    ttmkfdir  >  fonts.dir         &&
    cp  fonts.dir  fonts.scale
  fi
fi

if  spell_installed  fontconfig ; then
  fc-cache
fi
