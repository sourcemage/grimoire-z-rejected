cp usr/bin/cgc /usr/bin/ &&
cp -R usr/include/Cg /usr/include/ && 
if [ -d usr/lib64 ]; then
LIBDIR=usr/lib64
else
LIBDIR=usr/lib
fi &&
cp $LIBDIR/libCg*.so /usr/lib/ &&
cp -R usr/local/Cg /usr/share/doc/ &&
cp usr/share/man/man3/cg*.3 /usr/share/man/man3/
