INSTALL_DIR=${INSTALL_ROOT}/opt/teewars &&
mkdir $INSTALL_DIR &&
cp -v ${SPELL_DIRECTORY}/teewars ${SPELL_DIRECTORY}/teewars_srv \
      ${INSTALL_ROOT}/usr/games &&
cp -rv * $INSTALL_DIR &&
cd $INSTALL_DIR &&
find_and_scam &&
chmod 750 teewars teewars_srv
