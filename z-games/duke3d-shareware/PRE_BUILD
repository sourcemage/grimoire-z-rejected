warn_rejected &&
ask_continue_with_rejected &&

mkdir -p $SOURCE_DIRECTORY &&
cd      $SOURCE_DIRECTORY  &&
                                                                                
unpack  $SOURCE  ${MD5[0]}