INSTALL_DIR=${INSTALL_ROOT}/opt/warsow &&
mkdir $INSTALL_DIR &&
mv -v warsow ${INSTALL_ROOT}/usr/games &&
cp -rv * $INSTALL_DIR &&
chown -R root:games ${INSTALL_ROOT}/usr/games/warsow $INSTALL_DIR &&
chmod 750 ${INSTALL_ROOT}/usr/games/warsow $INSTALL_DIR
