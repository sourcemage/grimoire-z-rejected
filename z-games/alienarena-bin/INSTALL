INSTALL_DIRECTORY=$INSTALL_ROOT/opt/alienarena &&
mkdir -p $INSTALL_DIRECTORY &&
cp -rv * $INSTALL_DIRECTORY &&
cp -v $SPELL_DIRECTORY/{crded,crx,crx.sdl} $INSTALL_ROOT/usr/games &&
cd $INSTALL_DIRECTORY &&
find_and_scam &&
chmod 750 crded crx crx.sdl
