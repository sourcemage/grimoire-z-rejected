INSTALL_DIR=${INSTALL_ROOT}/opt/teeworlds &&
mkdir $INSTALL_DIR &&
cp -v ${SPELL_DIRECTORY}/teeworlds ${SPELL_DIRECTORY}/teeworlds_srv \
      ${INSTALL_ROOT}/usr/games &&
cp -rv * $INSTALL_DIR &&
cd $INSTALL_DIR &&
find_and_scam &&
chmod 750 teeworlds teeworlds_srv
