if  !  grep  -q  CONFIGURED  $SPELL_CONFIG;  then

  if  query  "Compile OpenGL helper program?" y;  then
    echo  'OPENGL="y"'  >>  $SPELL_CONFIG
  fi

  echo  'CONFIGURED="y"'  >>  $SPELL_CONFIG

fi
